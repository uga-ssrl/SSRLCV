stages:
  - build
  - test
  - evaluate

before_script:
  - export PATH=$PATH:/usr/local/cuda-8.0/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/snap/bin

compile:
  stage: build
  script: make

# Perform unit tests
unit_tests:
  stage: test
  script: ./util/CI/unit_test1.sh

