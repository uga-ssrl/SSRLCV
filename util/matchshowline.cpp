    // std::string show_matches = "python3 util/python/match_plotter.py --matches=" + dir_path + "/matches.txt ";
    // show_matches += "--image1=" + imagePaths[0];
    // show_matches += " --image2=" + imagePaths[1];
    // show_matches += " --numshow=" + std::to_string(5);
    // int exitCode = system(show_matches.c_str());
    // std::cout<<exitCode<<std::endl;

